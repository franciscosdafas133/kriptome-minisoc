{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://kriptome.internal/specifications/normalized_event.schema.json",
  "title": "NormalizedEvent",
  "description": "Forma normalizada de un evento de Wazuh, sin importar la familia de origen (SSH, Windows, FIM, M365, etc.). Es el contrato de ENTRADA del extractor de campos descrito en REGLAS_TUNEADAS/routing/diccionario_campos_enriquecimiento.csv. Los campos aquí corresponden a los verificados contra decoders/reglas reales de Wazuh v4.14.6 (ver REGLAS_TUNEADAS/GUIA_CAMPOS_ENRIQUECIMIENTO.docx); no se inventan campos que no existan en el evento real. NO es un schema implementado — es el contrato para cuando se construya el extractor (fuera de alcance de este repo, ver README).",
  "type": "object",
  "additionalProperties": false,
  "required": ["event_id", "tenant_id", "timestamp", "source", "rule_id", "rule_level"],
  "properties": {
    "event_id": {
      "type": "string",
      "description": "Identificador único del evento (equivalente a event_hash en el diseño de dedupe, doc 03 nivel de control 1). No es el rule_id."
    },
    "tenant_id": {
      "type": "string",
      "description": "Resuelto SIEMPRE por backend a partir de agent_id -> labels, nunca aceptado directo del frontend. Ver specifications/tenant_resolution.md."
    },
    "asset_id": {
      "type": ["string", "null"],
      "description": "Identificador del activo/equipo (label asset_id del agente). Null solo si el agente aún no fue provisto de labels (estado que debe generar cuarentena, ver tenant_resolution.md)."
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "Momento del evento en el origen, no de la ingesta."
    },
    "source": {
      "type": "string",
      "enum": ["wazuh_syslog", "wazuh_eventchannel", "wazuh_syscheck", "wazuh_rootcheck", "wazuh_sca", "wazuh_vulnerability_detector", "wazuh_virustotal", "wazuh_firewall", "wazuh_office365", "wazuh_agent_status"],
      "description": "Familia/mecanismo de origen del evento. No confundir con case_type (que es la clasificación de negocio)."
    },
    "case_type": {
      "type": ["string", "null"],
      "description": "Clasificación de negocio resuelta contra REGLAS_TUNEADAS/routing/routing_matrix.csv por rule_id. Null si el rule_id no está en la matriz (evento no enrutado; debe ir a cola de revisión, no descartarse en silencio)."
    },
    "rule_id": {
      "type": "string",
      "description": "ID de regla Wazuh que disparó (nativo o custom, p. ej. '5763' o '100012')."
    },
    "rule_level": {
      "type": "integer",
      "minimum": 0,
      "maximum": 16
    },
    "rule_groups": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Grupos de la regla, incluye los grupos kriptome_* de routing (kriptome_candidate, kriptome_critical, etc.) cuando aplica."
    },
    "source_ip": {
      "type": ["string", "null"],
      "description": "Campo srcip / win.eventdata.ipAddress / office365.ClientIP según familia. Ver diccionario_campos_enriquecimiento.csv para el mapeo exacto por familia."
    },
    "destination_ip": {
      "type": ["string", "null"],
      "description": "Campo dstip (firewall) o win.eventdata.destinationIp (Sysmon EID3)."
    },
    "target_user": {
      "type": ["string", "null"],
      "description": "Usuario objetivo/afectado: dstuser (SSH), win.eventdata.targetUserName (Windows/AD), office365.UserId (M365), srcuser (sudo, con la limitación documentada de que el comando ejecutado no es un campo separado)."
    },
    "file_path": {
      "type": ["string", "null"],
      "description": "syscheck.path (FIM) o win.eventdata.image / office365.SourceFileName según familia."
    },
    "file_hash": {
      "type": ["string", "null"],
      "description": "syscheck.sha256_after (FIM) o virustotal.source.file (AV). No se asume que siempre exista sha256 — algunas familias solo dan md5/sha1."
    },
    "cve": {
      "type": ["string", "null"],
      "description": "vulnerability.cve, verificado en rules/0520-vulnerability-detector_rules.xml del ruleset oficial."
    },
    "mitre_ids": {
      "type": "array",
      "items": { "type": "string" },
      "description": "IDs MITRE ATT&CK ya presentes en la regla que disparó (bloque <mitre><id> del XML)."
    },

    "asset_alias": {
      "type": ["string", "null"],
      "description": "Label agent.labels.asset_alias -- nombre de negocio del equipo (p. ej. 'Servidor de facturación'). Es el único identificador que debe llegar al cliente (ver specifications/client_notification.schema.json). Null si el agente aún no tiene ese label configurado."
    },
    "hostname": {
      "type": ["string", "null"],
      "description": "win.system.computer / nombre real del host que reporta. Dato técnico interno -- nunca se envía al cliente en el cuerpo principal del correo (solo en 'detalle_soporte_tecnico')."
    },
    "environment": {
      "type": ["string", "null"],
      "enum": ["prod", "staging", "dev", null],
      "description": "Label agent.labels.environment (ver REGLAS_TUNEADAS/agent-conf/CLI-001_agent.conf.example)."
    },
    "criticality": {
      "type": ["string", "null"],
      "enum": ["low", "medium", "high", "critical", null],
      "description": "Label agent.labels.criticality del activo. Determina el routing (ver policies/case_types/*.yaml)."
    },
    "business_service": {
      "type": ["string", "null"],
      "description": "Servicio de negocio que depende de este activo. No existe hoy como campo nativo de Wazuh -- requiere que el onboarding lo registre en la CMDB (ver DOCUMENTOS/03_FLUJOS_OPERATIVOS.docx §5)."
    },

    "first_seen_at": {
      "type": ["string", "null"],
      "format": "date-time",
      "description": "Primer evento del patrón correlacionado (p. ej. el primer intento de una racha de fuerza bruta). Distinto de 'timestamp', que es de ESTE evento puntual."
    },
    "last_seen_at": {
      "type": ["string", "null"],
      "format": "date-time",
      "description": "Evento más reciente del mismo patrón correlacionado."
    },
    "failed_attempt_count": {
      "type": ["integer", "null"],
      "minimum": 0,
      "description": "Para correlaciones de fuerza bruta (frequency/timeframe de reglas como 5763, 60204, 100010, 100011): cuántos intentos fallidos lleva contados la regla al momento de este evento."
    },
    "successful_login_count": {
      "type": ["integer", "null"],
      "minimum": 0,
      "description": "Accesos exitosos detectados dentro de la ventana de correlación (relevante para 100012/100013: éxito tras fuerza bruta)."
    },
    "logon_type": {
      "type": ["string", "null"],
      "description": "win.eventdata.logonType (p. ej. '3'=red, '10'=RDP). Verificado en rules/0580-win-security_rules.xml. Null fuera de eventos de logon de Windows."
    },
    "workstation_name": {
      "type": ["string", "null"],
      "description": "win.eventdata.workstationName -- estación de origen del intento de logon, cuando el decoder la expone."
    },

    "risk_score": {
      "type": ["number", "null"],
      "minimum": 0,
      "maximum": 100,
      "description": "Score de riesgo compuesto. CONTRATO DE DATOS, NO IMPLEMENTADO: no existe hoy un modelo/clasificador construido en este repo que calcule este valor -- debe quedar null hasta que exista, nunca rellenarse con un número inventado."
    },
    "risk_factors": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Motivos concretos y trazables a datos reales del evento (p. ej. 'criticality=critical', 'cve en catálogo CISA KEV', 'segundo intento en 24h'). Vacío si no hay factores calculados todavía."
    },

    "raw_event_reference": {
      "type": "string",
      "description": "Puntero al evento original en el Indexer (no el log completo embebido aquí) -- para que el operador o la App puedan recuperar el detalle completo sin que este objeto normalizado cargue el payload entero."
    },
    "enrichment_pending": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": ["geoip", "ip_reputation", "hash_reputation", "domain_reputation", "cve_description_es", "office365_recordtype_translation", "cisa_kev"]
      },
      "description": "Lista de enriquecimientos externos que este evento requiere y AÚN no se resolvieron (ver columna 'EnriquecimientoExternoNecesario' del diccionario). Ninguno de estos es nativo de Wazuh. 'cisa_kev' se agrega SOLO cuando 'cve' no es null -- ver specifications/enrichment_result.schema.json y policies/case_types/VULNERABILITY_KEV.yaml para la regla completa (la consulta KEV se hace únicamente con un CVE válido entregado por Wazuh, nunca de forma especulativa)."
    }
  }
}
